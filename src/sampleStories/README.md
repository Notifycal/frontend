This entire folder was automatically created by Storybook upon setup and it just contains some default
stories so it's not totally empty just after installation.

It can be deleted at any time.

Update `eslint.config.js` and `.prettierignore` once the folder is deleted.
